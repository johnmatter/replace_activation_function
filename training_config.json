{
  "activation_type_to_replace": "relu",
  "training": {
    "epochs": 10,
    "batch_size": 32,
    "learning_rate": 0.0001,
    "optimizer": "adam",
    "loss": "categorical_crossentropy",
    "metrics": ["accuracy"],
    "validation_split": 0.2
  },
  "retraining": {
    "batch_mode": {
      "batch_size": 16,
      "activation_replacement_batch_size": 1,
      "epochs": 2
    },
    "iterative_mode": {
      "batch_size": 16,
      "epochs": 2
    },
    "all_mode": {
      "batch_size": 16,
      "epochs": 2
    }
  },
  "prediction_decoder": {
    "top_k": 1000
  }
}
