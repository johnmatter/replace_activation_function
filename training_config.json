{
  "training": {
    "epochs": 10,
    "batch_size": 16,
    "learning_rate": 0.001,
    "optimizer": "adam",
    "loss": "categorical_crossentropy",
    "metrics": ["accuracy"],
    "validation_split": 0.2
  },
  "retraining": {
    "batch_mode": {
      "batch_size": 16,
      "epochs": 10
    },
    "iterative_mode": {
      "batch_size": 16,
      "epochs": 10
    },
    "all_mode": {
      "batch_size": 16,
      "epochs": 10
    }
  },
  "prediction_decoder": {
    "top_k": 1000
  }
}
