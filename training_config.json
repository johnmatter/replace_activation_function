{
  "activation_type_to_replace": "relu",
  "training": {
    "epochs": 3,
    "batch_size": 32,
    "learning_rate": 0.001,
    "optimizer": "adam",
    "loss": "categorical_crossentropy",
    "metrics": ["accuracy"],
    "validation_split": 0.1
  },
  "retraining": {
    "retraining_type": "iterative",
    "batch_size": 32,
    "activation_replacement_batch_size": 1,
    "epochs": 5
  },
  "prediction_decoder": {
    "top_k": 1000
  }
}
